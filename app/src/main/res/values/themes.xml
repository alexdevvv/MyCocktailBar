<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyCocktailBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyCocktailBar" parent="Base.Theme.MyCocktailBar" />

    <style name="MyFloatingActionButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="android:backgroundTint">@color/base_blue</item>
        <item name="android:tint">@color/white</item>
<!--        <item name="android:src">@drawable/baseline_add_24</item>-->
<!--        <item name="android:scaleType">center</item>-->
        <!-- Другие параметры и атрибуты, по вашему выбору -->
    </style>

    <style name="MySaveButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/blue</item>
        <item name="android:textColor">@android:color/white</item>
        <!-- Другие настройки стиля кнопки -->
    </style>

    <style name="MyAddButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/blue</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="srcCompat">@drawable/baseline_add_circle_24</item>
        <item name="android:src">@drawable/baseline_add_circle_24</item>
        <!-- Другие настройки стиля кнопки -->
    </style>

    <style name="MyCancelButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/blue</item>
        <item name="strokeColor">@color/blue</item>
        <item name="strokeWidth">2dp</item>

        <!-- Другие настройки стиля кнопки -->
    </style>
</resources>